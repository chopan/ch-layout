/*
    Document   : structure.css
    Created on : 27/09/2010, 09:32:52 AM
    Author     : cfgarcia
    Description:
        Estilo de la estructura del sistema.
*/
@import "variables";

html{
  font-family:"Helvetica Neue", Arial, Helvetica, sans-serif;
  color:#222;
  font-size:10pt;
  background-color:$bgColor;
  margin:0;
  background-image: url(../images/bg.png);
  background-repeat: repeat-x;
  background-position: 0px $headerHeight;
}
body{
  margin:0;
  background-image: url(../images/header_bg.png);
  background-repeat: repeat-x;
}
#HEADER{
  width: $headerWidth;
  height: $headerHeight;
  margin:auto;
  position: relative;
}
#STRUCTURE{
  width: $strucWidth;
  border: 1px solid #FF00EA;
  margin:auto;

    #HEADER_IN{
      background-color:#fff;
      border: 1px solid #0089FF;
    }
    
    #CONTENT{
      width:100%;
      position:relative;
      border: 1px solid #0089FF;
      overflow: hidden;
      background-color: #fff;
   
        #SIDEBAR_LEFT{
          width: content-width(left);
          display: content-onoff($sidebar-left-onoff);
          float: left;
          border: 0px solid #75C403;
          background-color:#75C403;
          padding:1% 1.2%;
          margin: 1% 0 1% 1%;
        }
        #CONTENT_BODY{
          width: content-width(body);
          display: content-onoff($content-body-onoff);
          float: left;
          border: 0px solid #75C403;
          background-color:#e9e9e9;
          margin: 1% 0 1% 1%;
          padding:1% 2.5%;
        }
        #SIDEBAR_RIGHT{
          width: content-width(right);
          display: content-onoff($sidebar-right-onoff);
          float: left;
          border: 0px solid #75C403;
          background-color:#75C403;
          padding:1% 1.2%;
          margin: 1% 0 1% 1%;
        }
    }
    p{
      margin:0;
      padding:0px;
    }
    #FOOTER_IN{
      background-color:#fff;
      border: 1px solid #0089FF;
    }
}
#FOOTER{
  width: $footerWidth;
  height: $footerHeight;
  margin:auto;
  position: relative;
}
